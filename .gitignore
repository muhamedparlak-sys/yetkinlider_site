# Ortam değişkenleri (local dev)
.env
.env.*
!.env.example

# Supabase service_role key — asla commit edilmez
.supabase/

# Claude Code
.claude/
.claude-code/

# Git worktrees (ana repo dışında tutulur)
worktrees/

# Node (eğer ileride build pipeline eklenirse)
node_modules/
dist/
.npm

# OS kalıntıları
.DS_Store
Thumbs.db

# Python (mevcut .py scriptler için)
__pycache__/
*.pyc

# ── Ticari IP — GitHub'da yayınlanmaz, Supabase'de tutulur ──────────────────
# Mesele tipi şablonları (PSG içerik motoru) — artık repo'da tutulur
# data/mesele-tipleri/
# Sözlük (glossary)
data/glossary.json
# Migration scriptleri (service key içerebilir)
scripts/mesele_batch_*.sql
scripts/glossary_insert.sql
scripts/insert_mesele_tipleri.sql
scripts/insert_glossary.sql
scripts/*.b64
